import classes from "./Loader.module.scss";

const Loader = () => {
  return (
    <div class={classes["multi-ripple"]}>
      <div></div>
      <div></div>
    </div>
  );
};

export default Loader;
