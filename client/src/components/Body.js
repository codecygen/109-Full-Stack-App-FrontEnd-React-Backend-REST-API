import classes from "./Body.module.scss";

const Body = () => {
  return (
    <main className={classes.body}>
      <div>Body</div>
      <button>Aras</button>
    </main>
  );
};

export default Body;
